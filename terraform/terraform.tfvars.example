# DigitalOcean API token (from https://cloud.digitalocean.com/account/api/tokens)
do_token = "your-digitalocean-token-here"

# Region (nyc3, sfo3, lon1, etc.)
region = "nyc3"

# Droplet sizes
api_droplet_size = "s-2vcpu-2gb"
db_droplet_size  = "s-2vcpu-4gb"

# Domain name (required — no default; change this to migrate to a new domain)
domain_name = "parley.example.com"
# Cloudflare zone (parent domain). In CI this is auto-detected from the CF API.
# For local terraform runs, set this to the zone that domain_name lives in.
cf_zone = "example.com"

# Number of API droplets
api_count = 3

# SSH private key path
ssh_private_key = "~/.ssh/id_rsa"

# PostgreSQL password (CHANGE THIS!)
db_password = "change-me-in-production"

# JWT secret (CHANGE THIS! - generate with: openssl rand -base64 32)
jwt_secret = "change-me-use-openssl-rand-base64-32"

# Impersonation JWT secret — MUST differ from jwt_secret (F-admin-jwt-secret).
# Admin container signs with this; api verifies. Generate with: openssl rand -base64 32
impersonation_jwt_secret = "change-me-use-openssl-rand-base64-32"

# Git repository URL
repo_url = "https://github.com/yourusername/parley.git"

# Giphy API key (from https://developers.giphy.com)
giphy_api_key = "your-giphy-api-key-here"

# Cloudflare API token (zone:read + dns:edit permissions for x86-64.com)
# Set this as CLOUDFLARE_API_TOKEN env var instead of in this file:
#   export CLOUDFLARE_API_TOKEN="cfk_..."
# In CI it is read automatically from the CLOUDFLARE_API_KEY GitHub secret.