import{_ as s,o as i,c as t,ag as a}from"./chunks/framework.BYYsWsaA.js";const m=JSON.parse('{"title":"Permissions Overhaul Design Spec","description":"","frontmatter":{},"headers":[],"relativePath":"specs/2026-03-15-permissions-overhaul-design.md","filePath":"specs/2026-03-15-permissions-overhaul-design.md"}'),n={name:"specs/2026-03-15-permissions-overhaul-design.md"};function r(o,e,d,l,h,c){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="permissions-overhaul-design-spec" tabindex="-1">Permissions Overhaul Design Spec <a class="header-anchor" href="#permissions-overhaul-design-spec" aria-label="Permalink to &quot;Permissions Overhaul Design Spec&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Replace Parley&#39;s 6-bit server-wide permission system with a full Discord-style permission model: 42 permission bits, immutable <code>@everyone</code> baseline role, strict positional role hierarchy, tri-state (allow/deny/inherit) permission overwrites at category, channel, and member levels, category sync, and View Channel visibility filtering.</p><h2 id="permission-bitfield" tabindex="-1">Permission Bitfield <a class="header-anchor" href="#permission-bitfield" aria-label="Permalink to &quot;Permission Bitfield&quot;">​</a></h2><p>All permissions stored as <code>int64</code> bitfields (64 bits available, 42 defined, rest reserved).</p><h3 id="server-only-permissions-bits-0–13" tabindex="-1">Server-Only Permissions (bits 0–13) <a class="header-anchor" href="#server-only-permissions-bits-0–13" aria-label="Permalink to &quot;Server-Only Permissions (bits 0–13)&quot;">​</a></h3><p>These apply server-wide and cannot be overridden per-channel.</p><table tabindex="0"><thead><tr><th>Bit</th><th>Constant</th><th>Description</th></tr></thead><tbody><tr><td>0</td><td><code>Administrator</code></td><td>Bypasses all permission checks. Grants all permissions.</td></tr><tr><td>1</td><td><code>ManageServer</code></td><td>Edit server name, icon, settings</td></tr><tr><td>2</td><td><code>ManageRoles</code></td><td>Create/edit/delete roles below own highest role</td></tr><tr><td>3</td><td><code>ManageChannels</code></td><td>Create/edit/delete/reorder channels and categories</td></tr><tr><td>4</td><td><code>KickMembers</code></td><td>Kick members with lower role position</td></tr><tr><td>5</td><td><code>BanMembers</code></td><td>Ban members with lower role position</td></tr><tr><td>6</td><td><code>ManageNicknames</code></td><td>Change other members&#39; nicknames</td></tr><tr><td>7</td><td><code>ChangeNickname</code></td><td>Change own nickname</td></tr><tr><td>8</td><td><code>CreateInvite</code></td><td>Create server invite links</td></tr><tr><td>9</td><td><code>ViewAuditLog</code></td><td>View the server audit log (future)</td></tr><tr><td>10</td><td><code>ManageWebhooks</code></td><td>Create/edit/delete webhooks (future)</td></tr><tr><td>11</td><td><code>ManageExpressions</code></td><td>Manage custom emoji/stickers (future)</td></tr><tr><td>12</td><td><code>ManageEvents</code></td><td>Create/edit/delete server events (future)</td></tr><tr><td>13</td><td><code>ModerateMember</code></td><td>Timeout members (future)</td></tr></tbody></table><p>Bits 14–15 reserved.</p><h3 id="channel-permissions-—-text-bin-bits-16–31" tabindex="-1">Channel Permissions — Text &amp; Bin (bits 16–31) <a class="header-anchor" href="#channel-permissions-—-text-bin-bits-16–31" aria-label="Permalink to &quot;Channel Permissions — Text &amp; Bin (bits 16–31)&quot;">​</a></h3><p>These can be overridden per-category, per-channel, or per-member.</p><table tabindex="0"><thead><tr><th>Bit</th><th>Constant</th><th>Description</th></tr></thead><tbody><tr><td>16</td><td><code>ViewChannel</code></td><td>See the channel in the sidebar. Denying hides it entirely.</td></tr><tr><td>17</td><td><code>SendMessages</code></td><td>Send messages in text channels</td></tr><tr><td>18</td><td><code>EmbedLinks</code></td><td>Links auto-embed previews</td></tr><tr><td>19</td><td><code>AttachFiles</code></td><td>Upload files and images</td></tr><tr><td>20</td><td><code>AddReactions</code></td><td>Add new reactions to messages</td></tr><tr><td>21</td><td><code>MentionEveryone</code></td><td>Use <code>@everyone</code> and <code>@here</code></td></tr><tr><td>22</td><td><code>ManageMessages</code></td><td>Delete others&#39; messages, pin messages</td></tr><tr><td>23</td><td><code>ReadMessageHistory</code></td><td>Read messages sent before joining</td></tr><tr><td>24</td><td><code>UseExternalEmoji</code></td><td>Use emoji from other servers (future)</td></tr><tr><td>25</td><td><code>PinMessages</code></td><td>Pin messages in the channel</td></tr><tr><td>26</td><td><code>ManageThreads</code></td><td>Manage threads/forum posts (future)</td></tr><tr><td>27</td><td><code>CreatePublicThreads</code></td><td>Create public threads (future)</td></tr><tr><td>28</td><td><code>SendMessagesInThreads</code></td><td>Send messages in threads (future)</td></tr><tr><td>29</td><td><code>CreatePosts</code></td><td>Create bin posts</td></tr><tr><td>30</td><td><code>ManagePosts</code></td><td>Edit/delete others&#39; bin posts</td></tr><tr><td>31</td><td><code>ManageTags</code></td><td>Create/delete bin channel tags</td></tr></tbody></table><h3 id="channel-permissions-—-voice-bits-32–41" tabindex="-1">Channel Permissions — Voice (bits 32–41) <a class="header-anchor" href="#channel-permissions-—-voice-bits-32–41" aria-label="Permalink to &quot;Channel Permissions — Voice (bits 32–41)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Bit</th><th>Constant</th><th>Description</th></tr></thead><tbody><tr><td>32</td><td><code>Connect</code></td><td>Connect to voice channels (enforced at voice join)</td></tr><tr><td>33</td><td><code>Speak</code></td><td>Speak in voice channels (enforced at voice join)</td></tr><tr><td>34</td><td><code>MuteMembers</code></td><td>Server-mute others in voice (future enforcement)</td></tr><tr><td>35</td><td><code>DeafenMembers</code></td><td>Server-deafen others in voice (future enforcement)</td></tr><tr><td>36</td><td><code>MoveMembers</code></td><td>Move members between voice channels (future enforcement)</td></tr><tr><td>37</td><td><code>UseVAD</code></td><td>Use voice activity detection vs push-to-talk (future enforcement)</td></tr><tr><td>38</td><td><code>PrioritySpeaker</code></td><td>Priority speaker in voice (future)</td></tr><tr><td>39</td><td><code>Stream</code></td><td>Share screen / go live (future)</td></tr><tr><td>40</td><td><code>UseSoundboard</code></td><td>Use soundboard sounds (future)</td></tr><tr><td>41</td><td><code>SendVoiceMessages</code></td><td>Send voice messages (future)</td></tr></tbody></table><p>Bits 42–63 reserved for future use.</p><h3 id="bitmask-constants" tabindex="-1">Bitmask Constants <a class="header-anchor" href="#bitmask-constants" aria-label="Permalink to &quot;Bitmask Constants&quot;">​</a></h3><ul><li><code>ALL_PERMISSIONS</code> — all 42 bits set</li><li><code>CHANNEL_PERMISSION_MASK</code> — bits 16–41 (the only bits valid in overwrites)</li><li><code>DEFAULT_EVERYONE_PERMISSIONS</code> — <code>ViewChannel | SendMessages | ReadMessageHistory | AddReactions | EmbedLinks | AttachFiles | Connect | Speak | UseVAD | ChangeNickname | CreateInvite | CreatePosts</code></li></ul><h2 id="role-system" tabindex="-1">Role System <a class="header-anchor" href="#role-system" aria-label="Permalink to &quot;Role System&quot;">​</a></h2><h3 id="everyone-role" tabindex="-1">@everyone Role <a class="header-anchor" href="#everyone-role" aria-label="Permalink to &quot;@everyone Role&quot;">​</a></h3><p>Every server has an immutable <code>@everyone</code> role:</p><ul><li><strong>Identified by <code>is_everyone = true</code></strong> column on <code>server_roles</code> (avoids BIGSERIAL ID conflicts). One per server, enforced by a partial unique index.</li><li><strong>Position = 0</strong> (always the lowest role)</li><li>Cannot be deleted or renamed</li><li>Not explicitly assigned via <code>server_member_roles</code> — every member has it implicitly. The permission computation fetches the <code>@everyone</code> role separately via <code>WHERE server_id = $1 AND is_everyone = true</code>, then ORs in the member&#39;s explicitly assigned roles from the join table.</li><li>Permissions editable by server owner / users with ManageServer</li><li>Created automatically when a server is created</li><li><strong>Migration</strong>: add <code>is_everyone BOOLEAN NOT NULL DEFAULT FALSE</code> column to <code>server_roles</code>. Seed an <code>@everyone</code> role for every existing server that doesn&#39;t have one. Rename any existing roles named <code>@everyone</code> to <code>everyone</code> before seeding to avoid the UNIQUE(server_id, name) constraint.</li></ul><h3 id="role-hierarchy" tabindex="-1">Role Hierarchy <a class="header-anchor" href="#role-hierarchy" aria-label="Permalink to &quot;Role Hierarchy&quot;">​</a></h3><p>Roles ordered by <code>position</code> (higher number = more powerful). Strictly enforced:</p><ul><li><strong>Role management</strong>: Can only create/edit/delete roles with position below your highest role</li><li><strong>Permission granting</strong>: Can only grant permissions you yourself have</li><li><strong>Kick/Ban</strong>: Can only kick/ban members whose highest role position is below yours</li><li><strong>Role assignment</strong>: Can only assign/remove roles below your highest role</li><li><strong>Owner bypass</strong>: Server owner bypasses all hierarchy checks, always has all permissions</li></ul><h3 id="base-permission-computation" tabindex="-1">Base Permission Computation <a class="header-anchor" href="#base-permission-computation" aria-label="Permalink to &quot;Base Permission Computation&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>compute_base_permissions(member, server):</span></span>
<span class="line"><span>    if server.owner_id == member.user_id:</span></span>
<span class="line"><span>        return ALL_PERMISSIONS</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    perms = everyone_role.permissions</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    for role in member.roles:</span></span>
<span class="line"><span>        perms |= role.permissions</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    if perms &amp; Administrator:</span></span>
<span class="line"><span>        return ALL_PERMISSIONS</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    return perms</span></span></code></pre></div><h2 id="permission-overwrites" tabindex="-1">Permission Overwrites <a class="header-anchor" href="#permission-overwrites" aria-label="Permalink to &quot;Permission Overwrites&quot;">​</a></h2><h3 id="overwrite-model" tabindex="-1">Overwrite Model <a class="header-anchor" href="#overwrite-model" aria-label="Permalink to &quot;Overwrite Model&quot;">​</a></h3><p>Each overwrite links a target (role or user) to a channel (or category) with explicit <code>allow</code> and <code>deny</code> bitfields.</p><table tabindex="0"><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>id</td><td>BIGSERIAL PK</td><td></td></tr><tr><td>channel_id</td><td>BIGINT FK → channels</td><td>The channel or category</td></tr><tr><td>target_type</td><td>SMALLINT NOT NULL</td><td>0 = role, 1 = member</td></tr><tr><td>target_id</td><td>BIGINT NOT NULL</td><td>Role ID or User ID</td></tr><tr><td>allow</td><td>BIGINT NOT NULL DEFAULT 0</td><td>Permissions explicitly granted</td></tr><tr><td>deny</td><td>BIGINT NOT NULL DEFAULT 0</td><td>Permissions explicitly denied</td></tr><tr><td>UNIQUE(channel_id, target_type, target_id)</td><td></td><td>One overwrite per target per channel</td></tr></tbody></table><p>Only channel-scoped permissions (bits 16–41) are valid in overwrites. Server-only permission bits (0–13) are masked out before storage.</p><h3 id="tri-state-logic" tabindex="-1">Tri-State Logic <a class="header-anchor" href="#tri-state-logic" aria-label="Permalink to &quot;Tri-State Logic&quot;">​</a></h3><p>For each permission bit in an overwrite:</p><ul><li><strong>Allow (green)</strong>: Bit set in <code>allow</code>, not in <code>deny</code> → explicitly granted</li><li><strong>Deny (red)</strong>: Bit set in <code>deny</code>, not in <code>allow</code> → explicitly denied</li><li><strong>Inherit (grey)</strong>: Bit set in neither → falls through to the level above</li></ul><p>A bit should never be set in both <code>allow</code> and <code>deny</code> simultaneously. The API enforces this by clearing a bit from <code>deny</code> when setting it in <code>allow</code> and vice versa.</p><h3 id="channel-permission-computation" tabindex="-1">Channel Permission Computation <a class="header-anchor" href="#channel-permission-computation" aria-label="Permalink to &quot;Channel Permission Computation&quot;">​</a></h3><p>Matches Discord&#39;s algorithm exactly:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>compute_channel_permissions(member, channel, server):</span></span>
<span class="line"><span>    base = compute_base_permissions(member, server)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    if base &amp; Administrator:</span></span>
<span class="line"><span>        return ALL_PERMISSIONS</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    perms = base</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # Step 1: @everyone overwrites on this channel</span></span>
<span class="line"><span>    everyone_ow = get_overwrite(channel_id, role, server.id)</span></span>
<span class="line"><span>    if everyone_ow:</span></span>
<span class="line"><span>        perms &amp;= ~everyone_ow.deny</span></span>
<span class="line"><span>        perms |= everyone_ow.allow</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # Step 2: Role overwrites (combined across all member&#39;s roles)</span></span>
<span class="line"><span>    role_allow = 0</span></span>
<span class="line"><span>    role_deny = 0</span></span>
<span class="line"><span>    for role in member.roles:</span></span>
<span class="line"><span>        ow = get_overwrite(channel_id, role, role.id)</span></span>
<span class="line"><span>        if ow:</span></span>
<span class="line"><span>            role_allow |= ow.allow</span></span>
<span class="line"><span>            role_deny |= ow.deny</span></span>
<span class="line"><span>    perms &amp;= ~role_deny</span></span>
<span class="line"><span>    perms |= role_allow</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # Step 3: Member-specific overwrite (highest priority)</span></span>
<span class="line"><span>    member_ow = get_overwrite(channel_id, member, member.user_id)</span></span>
<span class="line"><span>    if member_ow:</span></span>
<span class="line"><span>        perms &amp;= ~member_ow.deny</span></span>
<span class="line"><span>        perms |= member_ow.allow</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    return perms</span></span></code></pre></div><h3 id="implicit-denials" tabindex="-1">Implicit Denials <a class="header-anchor" href="#implicit-denials" aria-label="Permalink to &quot;Implicit Denials&quot;">​</a></h3><p>Enforced in computation, not in storage:</p><ul><li>Denying <code>ViewChannel</code> → all other channel permissions denied</li><li>Denying <code>SendMessages</code> → <code>MentionEveryone</code>, <code>AttachFiles</code>, <code>EmbedLinks</code> denied</li><li>Denying <code>Connect</code> → all voice permissions denied</li></ul><h3 id="category-sync" tabindex="-1">Category Sync <a class="header-anchor" href="#category-sync" aria-label="Permalink to &quot;Category Sync&quot;">​</a></h3><p>Channels in a category have a <code>synced</code> boolean (default <code>true</code>). Sync is <strong>write-time only</strong> — synced channels always have a physical copy of the category&#39;s overwrites in the <code>permission_overwrites</code> table. The permission computation algorithm does not need category awareness; it only reads the channel&#39;s own overwrites.</p><ul><li><strong>Synced</strong>: Channel&#39;s overwrites mirror the parent category&#39;s overwrites exactly. When category overwrites change, all synced children have their overwrites replaced with the category&#39;s current set (write-time propagation).</li><li><strong>Desynced</strong>: Channel has independently modified overwrites. Category changes do not propagate.</li><li>A channel desyncs automatically when its overwrites are directly modified.</li><li>A desynced channel can be resynced by setting <code>synced: true</code> via the API — this replaces its overwrites with the category&#39;s current overwrites.</li><li>New channels created in a category start synced, copying the category&#39;s overwrites.</li></ul><h3 id="view-channel-visibility" tabindex="-1">View Channel Visibility <a class="header-anchor" href="#view-channel-visibility" aria-label="Permalink to &quot;View Channel Visibility&quot;">​</a></h3><ul><li>Channels where the user lacks <code>ViewChannel</code> are hidden from the channel list API response</li><li>Categories with zero visible children are also hidden</li><li>Server owner always sees all channels</li><li>Users with <code>Administrator</code> always see all channels</li><li>API endpoints for channels where the user lacks <code>ViewChannel</code> must return <strong>404 (not 403)</strong> to avoid leaking channel existence</li></ul><h2 id="database-changes" tabindex="-1">Database Changes <a class="header-anchor" href="#database-changes" aria-label="Permalink to &quot;Database Changes&quot;">​</a></h2><h3 id="new-table-permission-overwrites" tabindex="-1">New Table: <code>permission_overwrites</code> <a class="header-anchor" href="#new-table-permission-overwrites" aria-label="Permalink to &quot;New Table: \`permission_overwrites\`&quot;">​</a></h3><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> permission_overwrites (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BIGSERIAL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> PRIMARY KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    channel_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BIGINT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> REFERENCES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> channels(id) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON DELETE CASCADE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    target_type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SMALLINT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    target_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BIGINT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    allow </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BIGINT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    deny </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BIGINT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    UNIQUE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(channel_id, target_type, target_id)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> idx_perm_overwrites_channel </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> permission_overwrites(channel_id);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> idx_perm_overwrites_target </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> permission_overwrites(target_type, target_id);</span></span></code></pre></div><h3 id="modifications-to-existing-tables" tabindex="-1">Modifications to Existing Tables <a class="header-anchor" href="#modifications-to-existing-tables" aria-label="Permalink to &quot;Modifications to Existing Tables&quot;">​</a></h3><p><strong><code>channels</code></strong> — add sync tracking:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> channels </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> synced </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BOOLEAN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> TRUE;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Note: \`synced\` is only semantically meaningful for non-category channels.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Categories always have synced=true but it has no effect (they are the sync source, not target).</span></span></code></pre></div><p><strong><code>server_roles</code></strong> — add <code>is_everyone</code> flag:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> server_roles </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> is_everyone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">BOOLEAN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FALSE;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> UNIQUE INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> idx_server_roles_everyone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> server_roles(server_id) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> is_everyone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> TRUE;</span></span></code></pre></div><p>The partial unique index ensures at most one <code>@everyone</code> role per server. Permissions column is already <code>BIGINT</code>, no change needed.</p><p><strong><code>Channel</code> struct</strong> — add <code>Synced bool</code> field to the Go model and include it in API responses for <code>GET /servers/{serverID}/channels</code>.</p><h3 id="migration-remap-permission-bits" tabindex="-1">Migration: Remap Permission Bits <a class="header-anchor" href="#migration-remap-permission-bits" aria-label="Permalink to &quot;Migration: Remap Permission Bits&quot;">​</a></h3><p>Existing roles use bits 0–5 for 6 permissions. Remap to the new layout:</p><table tabindex="0"><thead><tr><th>Old Bit</th><th>Old Permission</th><th>New Bit</th><th>New Permission</th></tr></thead><tbody><tr><td>0 (1)</td><td>SendMessages</td><td>17</td><td>SendMessages</td></tr><tr><td>1 (2)</td><td>ManageMessages</td><td>22</td><td>ManageMessages</td></tr><tr><td>2 (4)</td><td>ManageChannels</td><td>3</td><td>ManageChannels</td></tr><tr><td>3 (8)</td><td>KickMembers</td><td>4</td><td>KickMembers</td></tr><tr><td>4 (16)</td><td>ManageServer</td><td>1</td><td>ManageServer</td></tr><tr><td>5 (32)</td><td>Administrator</td><td>0</td><td>Administrator</td></tr></tbody></table><p>SQL migration reads each role&#39;s <code>permissions</code>, remaps bits, writes the new value.</p><h3 id="migration-seed-everyone-roles" tabindex="-1">Migration: Seed @everyone Roles <a class="header-anchor" href="#migration-seed-everyone-roles" aria-label="Permalink to &quot;Migration: Seed @everyone Roles&quot;">​</a></h3><p>For every existing server, INSERT a role with <code>name = &#39;@everyone&#39;</code>, <code>position = 0</code>, <code>is_everyone = true</code>, <code>permissions = DEFAULT_EVERYONE_PERMISSIONS</code> (the <code>id</code> is auto-generated by BIGSERIAL — identification is via the <code>is_everyone</code> flag, not the ID). The <code>CreateServer</code> flow is updated to do this automatically for new servers.</p><h2 id="api-changes" tabindex="-1">API Changes <a class="header-anchor" href="#api-changes" aria-label="Permalink to &quot;API Changes&quot;">​</a></h2><h3 id="updated-endpoints" tabindex="-1">Updated Endpoints <a class="header-anchor" href="#updated-endpoints" aria-label="Permalink to &quot;Updated Endpoints&quot;">​</a></h3><p><strong><code>GET /servers/{id}/my-permissions</code></strong> — returns computed base permissions using new bitfield:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;int64 bitfield&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;is_owner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>The <code>permissions</code> value reflects the requesting member&#39;s computed base permissions (all roles OR&#39;d together). Example: a member with only <code>@everyone</code> defaults would have the <code>DEFAULT_EVERYONE_PERMISSIONS</code> value.</p><p><strong><code>GET /servers/{serverID}/channels</code></strong> — filters out channels where user lacks <code>ViewChannel</code>. Categories with zero visible children omitted. Owner sees all. Each channel includes <code>synced</code> boolean.</p><p><strong>Role CRUD</strong> (existing endpoints, updated enforcement):</p><ul><li><code>POST /servers/{id}/roles</code> — requires <code>ManageRoles</code>. Can only set permissions the actor has. Position must be below actor&#39;s highest role.</li><li><code>PATCH /servers/{id}/roles/{roleId}</code> — same constraints. Can&#39;t edit roles at or above own position. Can&#39;t rename or delete <code>@everyone</code>.</li><li><code>DELETE /servers/{id}/roles/{roleId}</code> — can&#39;t delete roles at or above own position. Can&#39;t delete <code>@everyone</code>.</li><li><code>POST /servers/{id}/members/{userID}/roles</code> — can only assign roles below own highest position.</li><li><code>DELETE /servers/{id}/members/{userID}/roles/{roleId}</code> — same constraint.</li></ul><p><strong>Kick/Ban</strong> — now enforces role hierarchy. Actor&#39;s highest role must be above target&#39;s highest role. The <code>CanKickBan</code> method in <code>server_members.go</code> is updated to fetch both actor&#39;s and target&#39;s highest role positions and compare them, returning 403 if the target outranks the actor.</p><h3 id="new-endpoints" tabindex="-1">New Endpoints <a class="header-anchor" href="#new-endpoints" aria-label="Permalink to &quot;New Endpoints&quot;">​</a></h3><p><strong>Channel permission overwrites:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET    /channels/{id}/overwrites                — List all overwrites for a channel/category</span></span>
<span class="line"><span>PUT    /channels/{id}/overwrites                — Upsert an overwrite: { target_type, target_id, allow, deny }</span></span>
<span class="line"><span>DELETE /channels/{id}/overwrites/{overwriteId}  — Remove an overwrite</span></span></code></pre></div><p>Requires <code>ManageRoles</code> <strong>or</strong> <code>ManageChannels</code> permission. Can only set permission bits the actor has. Server-only bits (0–13) are masked out.</p><p><strong>Per-channel permissions:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET    /channels/{id}/my-permissions            — Computed permissions for requesting user</span></span></code></pre></div><p>Returns <code>{ &quot;permissions&quot;: &quot;&lt;int64 bitfield&gt;&quot; }</code> with all overwrites applied.</p><h3 id="category-sync-behavior" tabindex="-1">Category Sync Behavior <a class="header-anchor" href="#category-sync-behavior" aria-label="Permalink to &quot;Category Sync Behavior&quot;">​</a></h3><p>When category overwrites are modified via <code>PUT /channels/{id}/overwrites</code>:</p><ol><li>Update the category&#39;s overwrite</li><li>Find all child channels with <code>synced = true</code></li><li>Replace their overwrites with the category&#39;s current full overwrite set (this bulk delete-then-copy is a service-internal transaction, not a separate API operation)</li></ol><p>When a channel&#39;s overwrites are directly modified:</p><ol><li>Set <code>synced = false</code> on the channel</li><li>Apply the overwrite change</li></ol><p>When <code>PUT /channels/{id}</code> includes <code>synced: true</code>:</p><ol><li>Delete the channel&#39;s current overwrites</li><li>Copy the parent category&#39;s overwrites</li><li>Set <code>synced = true</code></li></ol><h3 id="websocket-events" tabindex="-1">WebSocket Events <a class="header-anchor" href="#websocket-events" aria-label="Permalink to &quot;WebSocket Events&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Event</th><th>Payload</th><th>When</th></tr></thead><tbody><tr><td><code>CHANNEL_OVERWRITE_UPDATE</code></td><td><code>{ channel_id, overwrites[] }</code></td><td>Overwrite added/edited/removed</td></tr><tr><td><code>ROLE_UPDATE</code></td><td><code>{ role }</code></td><td>Role permissions or position changed</td></tr><tr><td><code>ROLE_DELETE</code></td><td><code>{ role_id, server_id }</code></td><td>Role deleted</td></tr></tbody></table><p>Existing events used: <code>CHANNEL_UPDATE</code> (sync state), <code>MEMBER_ROLE_UPDATE</code> (role assignment).</p><h3 id="api-documentation" tabindex="-1">API Documentation <a class="header-anchor" href="#api-documentation" aria-label="Permalink to &quot;API Documentation&quot;">​</a></h3><p>Full endpoint documentation for all permission-related endpoints will be added to <code>docs/endpoints/</code> covering request/response formats, error codes, and examples for bot developers.</p><h2 id="frontend-changes" tabindex="-1">Frontend Changes <a class="header-anchor" href="#frontend-changes" aria-label="Permalink to &quot;Frontend Changes&quot;">​</a></h2><h3 id="permission-utility-module" tabindex="-1">Permission Utility Module <a class="header-anchor" href="#permission-utility-module" aria-label="Permalink to &quot;Permission Utility Module&quot;">​</a></h3><p><code>frontend/src/lib/permissions.ts</code>:</p><ul><li><code>computeBasePermissions(member, roles, serverOwnerId)</code> — OR all role permissions, owner/admin bypass</li><li><code>computeChannelPermissions(basePerms, memberRoles, channelOverwrites, memberId)</code> — full overwrite algorithm</li><li><code>hasPermission(perms, bit)</code> — check a single bit</li><li>All 42 permission bit constants exported</li><li><code>CHANNEL_PERMISSION_MASK</code> — bits valid for overwrites</li></ul><h3 id="channel-list-filtering" tabindex="-1">Channel List Filtering <a class="header-anchor" href="#channel-list-filtering" aria-label="Permalink to &quot;Channel List Filtering&quot;">​</a></h3><p><code>ChannelList.tsx</code> filters channels through <code>computeChannelPermissions</code>. Channels where user lacks <code>ViewChannel</code> are hidden. Categories with zero visible children are hidden. Owner sees everything. Client-side filtering handles real-time overwrite updates without refetch.</p><h3 id="channel-settings-—-permissions-tab" tabindex="-1">Channel Settings — Permissions Tab <a class="header-anchor" href="#channel-settings-—-permissions-tab" aria-label="Permalink to &quot;Channel Settings — Permissions Tab&quot;">​</a></h3><p>New tab in channel/category settings (requires <code>ManageRoles</code> or <code>ManageChannels</code>):</p><ul><li><strong>Add role/member</strong>: search dropdown to add targets to the overwrite list</li><li><strong>Per-target permission grid</strong>: each permission as a row with three toggle states: <ul><li>Green checkmark = allow</li><li>Grey slash = inherit (neutral)</li><li>Red X = deny</li><li>Clicking cycles: inherit → allow → deny → inherit</li></ul></li><li><strong>Category header</strong>: organized by permission category (General, Text, Voice, Bin)</li><li><strong>Sync indicator</strong>: for channels in a category, shows sync status with &quot;Sync Now&quot; button for desynced channels</li><li>Save sends <code>PUT /channels/{id}/overwrites</code></li></ul><h3 id="role-management-modal-updates" tabindex="-1">Role Management Modal Updates <a class="header-anchor" href="#role-management-modal-updates" aria-label="Permalink to &quot;Role Management Modal Updates&quot;">​</a></h3><p><code>ManageRolesModal.tsx</code> — expand from 6 checkboxes to full permission set, organized by category:</p><ul><li><strong>General</strong>: Administrator, ManageServer, ManageRoles, ManageChannels, KickMembers, BanMembers, etc.</li><li><strong>Text</strong>: SendMessages, ManageMessages, EmbedLinks, AttachFiles, etc.</li><li><strong>Voice</strong>: Connect, Speak, MuteMembers, DeafenMembers, etc.</li><li><strong>Bin</strong>: CreatePosts, ManagePosts, ManageTags</li></ul><p>Role permissions remain binary (on/off), not tri-state. Tri-state is only for channel overwrites.</p><h3 id="permission-gated-ui-elements" tabindex="-1">Permission-Gated UI Elements <a class="header-anchor" href="#permission-gated-ui-elements" aria-label="Permalink to &quot;Permission-Gated UI Elements&quot;">​</a></h3><p>Hide or disable actions the user lacks permission for:</p><ul><li>Message input → <code>SendMessages</code></li><li>Delete others&#39; messages → <code>ManageMessages</code></li><li>Channel create/edit/delete → <code>ManageChannels</code></li><li>Kick/ban buttons → <code>KickMembers</code> / <code>BanMembers</code></li><li>Role management → <code>ManageRoles</code></li><li>Pin message → <code>PinMessages</code></li><li>Bin create post → <code>CreatePosts</code></li><li>Upload button → <code>AttachFiles</code></li><li>Reaction button → <code>AddReactions</code></li><li>@everyone/@here in message → <code>MentionEveryone</code></li></ul><h2 id="edge-cases-and-conflict-resolution" tabindex="-1">Edge Cases and Conflict Resolution <a class="header-anchor" href="#edge-cases-and-conflict-resolution" aria-label="Permalink to &quot;Edge Cases and Conflict Resolution&quot;">​</a></h2><h3 id="permission-conflicts-between-roles" tabindex="-1">Permission Conflicts Between Roles <a class="header-anchor" href="#permission-conflicts-between-roles" aria-label="Permalink to &quot;Permission Conflicts Between Roles&quot;">​</a></h3><p>When a user has multiple roles with overwrites on the same channel:</p><ol><li>All role denies are OR&#39;d together</li><li>All role allows are OR&#39;d together</li><li>Deny is applied first (<code>perms &amp;= ~deny</code>)</li><li>Allow is applied second (<code>perms |= allow</code>)</li></ol><p>This means <strong>allow wins</strong> when two roles conflict at the same level. This matches Discord.</p><h3 id="member-overwrite-vs-role-overwrite" tabindex="-1">Member Overwrite vs Role Overwrite <a class="header-anchor" href="#member-overwrite-vs-role-overwrite" aria-label="Permalink to &quot;Member Overwrite vs Role Overwrite&quot;">​</a></h3><p>Member-specific overwrites always win over role overwrites, regardless of direction. A member deny overrides a role allow, and a member allow overrides a role deny.</p><h3 id="administrator-bypass" tabindex="-1">Administrator Bypass <a class="header-anchor" href="#administrator-bypass" aria-label="Permalink to &quot;Administrator Bypass&quot;">​</a></h3><p>Users with <code>Administrator</code> permission (via any role) bypass all channel overwrite checks. They always have all permissions in all channels. The only exception: they cannot override the server owner.</p><h3 id="server-owner" tabindex="-1">Server Owner <a class="header-anchor" href="#server-owner" aria-label="Permalink to &quot;Server Owner&quot;">​</a></h3><p>The server owner always has all permissions everywhere. They cannot be kicked, banned, or have permissions restricted. They see all channels regardless of ViewChannel overwrites.</p><h3 id="deleted-roles" tabindex="-1">Deleted Roles <a class="header-anchor" href="#deleted-roles" aria-label="Permalink to &quot;Deleted Roles&quot;">​</a></h3><p>When a role is deleted, all overwrites referencing that role must be cleaned up at the application level (since <code>target_id</code> has no FK constraint — it can reference either roles or users). The role deletion service method must <code>DELETE FROM permission_overwrites WHERE target_type = 0 AND target_id = $role_id</code> as part of the delete operation. Members who had that role lose any permissions it granted.</p><h3 id="everyone-overwrite" tabindex="-1">@everyone Overwrite <a class="header-anchor" href="#everyone-overwrite" aria-label="Permalink to &quot;@everyone Overwrite&quot;">​</a></h3><p>The <code>@everyone</code> role can have channel overwrites like any other role. Since every member implicitly has this role, @everyone overwrites affect all non-administrator members. This is the primary way to restrict a channel (e.g., deny SendMessages on @everyone, then allow it for specific roles).</p><h2 id="out-of-scope" tabindex="-1">Out of Scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to &quot;Out of Scope&quot;">​</a></h2><ul><li>Audit log implementation (permission bit defined, feature is future)</li><li>Member timeout implementation (permission bit defined, feature is future)</li><li>Webhook management (permission bit defined, feature is future)</li><li>Thread permission enforcement (permission bits defined, threads are future)</li><li>Permission caching layer (optimize later if needed)</li></ul>`,123)])])}const g=s(n,[["render",r]]);export{m as __pageData,g as default};
